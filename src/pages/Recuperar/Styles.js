import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    paragraph: {
        marginHorizontal: 30,
        textAlign: 'center',
    },
    form: {
        alignSelf: 'stretch',
        marginHorizontal: 30,
        marginTop: 15,
    },
});

export default styles;